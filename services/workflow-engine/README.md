# UIOS Workflow Engine

The initial engine executes typed workflow graphs through registered node handlers. Durable queues, retries, human approvals and persistence can be added behind the same `WorkflowDefinition` and `WorkflowRun` contracts.
