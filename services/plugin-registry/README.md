# UIOS Plugin Registry

Plugins are installed through manifests with explicit permissions. A plugin cannot register providers or access platform capabilities until its requested permissions are approved. The registry is the seam for a future marketplace, signature verification and tenant-scoped installation.
