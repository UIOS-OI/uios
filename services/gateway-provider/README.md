# UIOS Gateway Provider

An implementation of the UIOS `ModelProvider` contract for any OpenAI-compatible gateway. It supports non-streaming chat, SSE streaming, model discovery and health checks without making the UIOS core import a vendor SDK.
