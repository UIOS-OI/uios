# UIOS Model Router

The router depends only on `@uios/contracts`. Providers implement `ModelProvider` and register themselves; core routing never imports OpenAI, Anthropic, Gemini or local-model SDKs.
